extends: default

rules:
  line-length:
    max: 160
    level: warning

  document-start: disable
  truthy: disable
  comments: disable

  indentation:
    spaces: 2
    indent-sequences: consistent
